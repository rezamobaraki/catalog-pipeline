[project]
name = "catalog-pipeline"
version = "0.1.0"
description = "CSV to JSON catalog transformer"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]
